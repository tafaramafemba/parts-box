<!-- filepath: /c:/Users/Taffy/OneDrive/Desktop/Repositories/parts-box/app/views/admin/reports/index.html.erb -->
<h1 class="page-title">PartsToGo Performance Report</h1>

<div class="report-container">
  <div class="report-section">
    <h2>1. Executive Summary</h2>
    <p><strong>Overview of Marketplace Performance</strong></p>
    <p><strong>Total Gross Merchandise Value (GMV):</strong> <%= number_to_currency(@total_revenue) %></p>
    <p><strong>Total Orders:</strong> <%= @total_orders %></p>
    <p><strong>Total Revenue (Commission + Fees):</strong> <%= number_to_currency(@total_commission_fees + @total_platform_fees) %></p>
    <p><strong>Active Sellers:</strong> <%= @active_sellers %></p>
    <p><strong>Active Buyers:</strong> <%= @total_customers %></p>
    <p><strong>Top Performing Categories:</strong> <%= @top_performing_categories.join(', ') %></p>
  </div>

  <div class="report-section">
    <h2>2. Marketplace Metrics</h2>
    <h3>Overall Performance</h3>
    <p><strong>Total Sales Volume:</strong> <%= number_to_currency(@total_revenue) %></p>
    <p><strong>New Sellers Onboarded:</strong> <%= @new_sellers_onboarded %></p>
    <p><strong>New Buyers Registered:</strong> <%= @new_buyers_registered %></p>
    <p><strong>Average Order Value (AOV):</strong> <%= number_to_currency(@average_order_value) %></p>

    <h3>Sales Breakdown by Category</h3>
    <table>
      <thead>
        <tr>
          <th>Category</th>
          <th>Sales Volume</th>
        </tr>
      </thead>
      <tbody>
        <% @sales_volume_by_category.each do |category, sales_volume| %>
          <tr>
            <td><%= category %></td>
            <td><%= number_to_currency(sales_volume) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="report-section">
    <h2>3. Seller Performance</h2>
    <h3>Payouts by Seller</h3>
    <table>
      <thead>
        <tr>
          <th>Seller Email</th>
          <th>Total Payout</th>
        </tr>
      </thead>
      <tbody>
        <% @payouts_by_seller.each do |seller, amount| %>
          <tr>
            <td><%= seller %></td>
            <td><%= number_to_currency(amount) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="report-section">
    <h2>4. Customer Insights</h2>
    <p><strong>Total Active Buyers:</strong> <%= @total_customers %></p>
  </div>
</div>

<style>
  .page-title {
    text-align: center;
    margin-bottom: 20px;
    font-size: 24px;
    font-weight: bold;
  }

  .report-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
    background-color: #f9f9f9;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  .report-section {
    margin-bottom: 40px;
  }

  .report-section h2 {
    font-size: 20px;
    margin-bottom: 10px;
    border-bottom: 2px solid #ddd;
    padding-bottom: 5px;
  }

  .report-section p, .report-section ul, .report-section ol {
    margin: 0 0 10px;
  }

  .report-section table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
  }

  .report-section table th, .report-section table td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: left;
  }

  .report-section table th {
    background-color: #f2f2f2;
  }
</style>