<% rating ||= 0 %> <!-- Ensure rating is defined -->
<% full_stars = rating.floor %> <!-- Use floor to avoid over-counting -->
<% empty_stars = 5 - full_stars %>

<div class="star-rating">
  <% full_stars.times { concat content_tag(:span, "★", class: "star full") } %>
</div>
